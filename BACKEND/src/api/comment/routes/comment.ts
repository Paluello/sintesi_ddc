/**
 * comment router - Disabled: Using native plugin comments API instead
 * The plugin provides routes at /api/comments/api::postit.postit:{id}
 */

export default {
  routes: [], // Routes disabled to use native plugin API
};

